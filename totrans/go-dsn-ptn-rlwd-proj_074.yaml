- en: Chapter 3. Go Control Flow
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 第 3 章。Go 控制流
- en: 'Go borrows several of its control flow syntax from the C-family of languages.
    It supports all of the expected control structures, including `if...else`, `switch`,
    `for` loop, and even `goto`. Conspicuously absent, though, are `while` or `do...while`
    statements. The following topics in this chapter examine Go''s control flow elements,
    some of which you may already be familiar with, and others that bring a new set
    of functionalities not found in other languages:'
  id: totrans-1
  prefs: []
  type: TYPE_NORMAL
  zh: Go 语言借鉴了 C 系列语言中的许多控制流语法。它支持所有预期的控制结构，包括 `if...else`、`switch`、`for` 循环，甚至 `goto`。然而，明显缺失的是
    `while` 或 `do...while` 语句。本章以下内容将探讨 Go 语言的控制流元素，其中一些你可能已经熟悉，而另一些则带来了一组在其他语言中找不到的新功能：
- en: The `if` statement
  id: totrans-2
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`if` 语句'
- en: The `switch` statement
  id: totrans-3
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`switch` 语句'
- en: The type `Switch`
  id: totrans-4
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 类型 `Switch`
- en: The `for` statement
  id: totrans-5
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`for` 语句'
