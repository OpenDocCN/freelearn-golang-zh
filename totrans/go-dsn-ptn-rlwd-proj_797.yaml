- en: Mapping the router handlers
  id: totrans-0
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 映射路由处理器
- en: 'Let''s update our `main.go` file by changing the `init` function to map the
    real handlers to HTTP paths:'
  id: totrans-1
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们更新我们的 `main.go` 文件，通过将 `init` 函数改为将实际处理器映射到 HTTP 路径：
- en: '[PRE0]'
  id: totrans-2
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
- en: You can also remove the now redundant `handleHello` handler function.
  id: totrans-3
  prefs: []
  type: TYPE_NORMAL
  zh: 你还可以移除现在多余的 `handleHello` 处理器函数。
